// stack (primitive), Heap(non primitive)

const fname= "kritika"
const sname= "srivastava"
// console.log(fname + sname + "dear");

// better way to write above things

// console.log(`hello my name is ${fname} and my surname is ${sname}`);

const gameName= "anneWithanE"
console.log(gameName[0]);

console.log(gameName.charAt(2));

const newstring= gameName.substring(0,4)
console.log(newstring);

// const anotherstring = gameName.slice(-8, 4)
// console.log(anotherstring);

const url= "https://free-url-shortener.rb.gy/"
console.log(url.replace('free', '-'));
console.log(url.includes('free'));
console.log(gameName.split('-'));

